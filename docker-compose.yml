services:
  pcl-registration:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: pcl-registration-demo
    volumes:
      - .:/app
      - build-cache:/app/build
      - ./data:/app/data
      - ./results:/app/results
    working_dir: /app

volumes:
  build-cache: