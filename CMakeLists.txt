set(CMAKE_CXX_STANDARD 17)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS ON)

cmake_minimum_required(VERSION 2.6 FATAL_ERROR)
project(pcl_test1)
find_package(PCL 1.7 REQUIRED)
include_directories(${PCL_INCLUDE_DIRS})
link_directories(${PCL_LIBRARY_DIRS})
add_definitions(${PCL_DEFINITIONS})
add_executable (main_registration main_registration.cpp)
target_link_libraries (main_registration ${PCL_LIBRARIES})

cmake_minimum_required(VERSION 2.6 FATAL_ERROR)
project(pcl_test1)
find_package(PCL 1.7 REQUIRED)
include_directories(${PCL_INCLUDE_DIRS})
link_directories(${PCL_LIBRARY_DIRS})
add_definitions(${PCL_DEFINITIONS})
add_executable (visualization ../include/visualization.cpp)
target_link_libraries (visualization ${PCL_LIBRARIES})

cmake_minimum_required(VERSION 2.6 FATAL_ERROR)
project(pcl_test1)
find_package(PCL 1.7 REQUIRED)
include_directories(${PCL_INCLUDE_DIRS})
link_directories(${PCL_LIBRARY_DIRS})
add_definitions(${PCL_DEFINITIONS})
add_executable (sift_pipeline sift_pipeline.cpp)
target_link_libraries (sift_pipeline ${PCL_LIBRARIES})

cmake_minimum_required(VERSION 2.6 FATAL_ERROR)
project(pcl_test1)
find_package(PCL 1.7 REQUIRED)
include_directories(${PCL_INCLUDE_DIRS})
link_directories(${PCL_LIBRARY_DIRS})
add_definitions(${PCL_DEFINITIONS})
add_executable (harris_pipeline harris_pipeline.cpp)
target_link_libraries (harris_pipeline ${PCL_LIBRARIES})

cmake_minimum_required(VERSION 2.6 FATAL_ERROR)
project(pcl_test1)
find_package(PCL 1.7 REQUIRED)
include_directories(${PCL_INCLUDE_DIRS})
link_directories(${PCL_LIBRARY_DIRS})
add_definitions(${PCL_DEFINITIONS})
add_executable (cpp_version ../include/cpp_version.cpp)
target_link_libraries (cpp_version ${PCL_LIBRARIES})
